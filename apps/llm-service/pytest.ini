[pytest]
# Os arquivos de teste deste serviço se chamam tests_*.py, fora do padrão do pytest. Sem esta linha,
# um arquivo novo de teste passa a existir e ninguém roda ele.
python_files = tests_*.py
testpaths = .
norecursedirs = .venv venv node_modules workspace data __pycache__ .pytest_cache
